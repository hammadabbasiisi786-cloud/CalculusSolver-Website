# TODO: Replace this with the PERMANENT backend URL before production deploy.
# The current value is a temporary LocalTunnel URL pointing to someone's local
# laptop. LocalTunnel URLs are ephemeral — they change on every restart and will
# break without warning. Once Member C deploys the FastAPI backend to a real
# host, update this to that host's URL (e.g. https://api.quantumlogicslimited.com).
VITE_API_URL=https://api.calculussolver.quantumlogicslimited.com